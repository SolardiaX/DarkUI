local E, C, L = select(2, ...):unpack()

------------------------------------------------------------------------
-- Class Power (oUF native element, no custom Diabolic bar)
------------------------------------------------------------------------
